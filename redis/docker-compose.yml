version: '3.7'

services:
  redis:
    image: redis
    ports:
        - 6379:6379
    volumes:
        - ./config/redis.conf:/redis.conf
    command: [ "redis-server", "/redis.conf" ]
    networks: 
      - net_app

networks:
  net_app:
    external: 
      name: net_app